class Photo {
  constructor(id, text, dbName, url, route) {
    (this.id = id),
      (this.text = text),
      (this.dbName = dbName),
      (this.url = url),
      (this.route = route);
  }
}
export default Photo;
